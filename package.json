{
  "name": "new-grad-jobs-2026",
  "version": "1.0.0",
  "description": "Template repository for Zapply job boards",
  "scripts": {
    "validate:secrets": "node .github/scripts/validate-secrets.js",
    "validate:imports": "node .github/scripts/validate-imports.js",
    "test": "npm run validate:secrets && npm run validate:imports"
  },
  "dependencies": {
    "axios": "^1.13.2"
  },
  "author": "zapplyjobs",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
